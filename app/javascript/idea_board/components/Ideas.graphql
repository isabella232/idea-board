query Ideas {
  ideas {
    id
    title
    body
    author
    votes
    voted
  }
}

query Idea($id: ID!) {
  idea(id: $id) {
    id
    title
    body
    votes
    author
    voted
  }
}

mutation CreateIdea($title: String!, $body: String) {
  createIdea(title: $title, body: $body) {
    idea {
      id
      title
      body
      author
      votes
      voted
    }
  }
}

mutation VoteFor($id: ID!) {
  voteFor(idea: $id) {
    idea {
      id
      votes
      voted
    }
  }
}
