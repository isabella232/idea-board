query Ideas {
  ideas {
    id
    title
    body
    votes
  }
}

query Idea($id: ID!) {
  idea(id: $id) {
    id
    title
    body
    votes
    author
  }
}

mutation CreateIdea($title: String!, $body: String) {
  createIdea(title: $title, body: $body) {
    idea {
      id
      title
      body
      votes
    }
  }
}

mutation VoteFor($id: ID!) {
  voteFor(idea: $id) {
    idea {
      id
      votes
    }
  }
}
